# Bible locale (Louis Segond 1910)

Généré par :

```bash
npm run bible:download
npm run bible:verify
```

Structure : `{bookId}/{chapter}.json` (ex. `jhn/3.json`).

En production : `BIBLE_LOCAL_ONLY=1` dans `.env`.
